import React from 'react';

export const Modal = ( { onClose }: any ) => {
   const style = {
      height: '30rem',
      width: '30rem'
   };

   return (
      <div style={ style }>MODAL</div>
   );
};